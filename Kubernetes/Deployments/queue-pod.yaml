apiVersion: v1
kind: Pod
metadata:
  labels:
    run: rabbitmq
  name: rabbitmq
spec:
  containers:
  - image: rabbitmq:3
    name: rabbitmq
    ports:
    - containerPort: 5672
  dnsPolicy: ClusterFirst
  restartPolicy: Always
